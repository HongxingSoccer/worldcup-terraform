# Copy to terraform.tfvars and fill in real values. terraform.tfvars is
# .gitignore'd; this example is the only checked-in version.

dev_account_id = "111111111111"

# aws_region   = "us-east-1"   # default
